%h2 Click on picture to set the mission target
-@urlarray.each do |url|
  =link_to pictures_path(picture: {answer: :true, url: url, mission_id: @mission.id}), method: :post, remote:true do
    =image_tag url
