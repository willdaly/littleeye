%div#newmissionform
  = form_tag(missions_path(@mission), method: "create") do
    = label_tag(:name, "code name")
    = text_field_tag(:name)
    = label_tag(:clue, "clue")
    = text_field_tag(:clue)
    = submit_tag("create mission")
